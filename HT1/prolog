    padre(Demetrio, Luis).
    padre(Demetrio, Flor).
    madre(Marta, Luis).
    madre(Marta, Flor).
    hermano(Luis, Flor).
    hermana(Flor, Luis).
    esposa(Luica, Luis).
    hermano(Luis, Fernando).
    hermana(Flor, Kevin).
    madre(Fidelia, Marta).

    % Reglas existentes
    abuelo(A, B) :- padre(A, X), padre(X, B).
    abuelo(A, B) :- madre(A, X), padre(X, B).

    hermano(A, B) :- padre(P, A), padre(P, B), A \= B.
    hermana(A, B) :- madre(M, A), madre(M, B), A \= B.

    es_padre(A, B) :- padre(A, B).
    es_madre(A, B) :- madre(A, B).